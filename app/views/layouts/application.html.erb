<!DOCTYPE html>
<html>
<head>

	<meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Paul Request</title>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= csrf_meta_tags %>

  <link href='http://fonts.googleapis.com/css?family=Signika:400,700,300' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag 'application', media: 'all' %>

</head>
<body>

	<header>
		<div class="container">
			<a class="logo-paul-request" href="/">Paul Request</a>

			<% if user_signed_in? %>
				<%= link_to('Sign Out', destroy_user_session_path, class: 'github-login', id: 'desktop', method: :delete) %>
			<% else %>
				<%= link_to('Sign in with Github', user_omniauth_authorize_path(:github), class: 'github-login', id: 'desktop') %>
			<% end %>
		</div>
	</header>

	<%= yield %>

	<%= javascript_include_tag 'application' %>

</body>
</html>
